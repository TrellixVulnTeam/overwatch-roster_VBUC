export class Character {
  constructor(public image: string, public name: string, public role: string, public difficulty: number, public health: number, public armor: number, public shield: number, public total: number, public place: string) { }
}
