export class Character {
  constructor(public name: string, public role: string, public dificulty: number, public health: number, public armor: number, public shield: number, public total: number, public place: string) { }
}
